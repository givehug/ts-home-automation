enum Endpoints {
  users = 'users/',
  userMe = 'users/me',
  userLogin = 'login',
  userLogout = 'users/me/token',
  homeSettings = 'homeSettings/',
  settings = 'settings/',
  devices = 'devices/',
  app = 'app/',
}

export default Endpoints;
