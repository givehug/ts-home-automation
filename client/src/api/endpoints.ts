enum Endpoints {
  users = 'users/',
  userMe = 'users/me',
  userLogin = 'login',
  userLogout = 'users/me/token',
  settings = 'settings/',
  devices = 'devices/',
  app = 'app/',
}

export default Endpoints;
